import { SubscribeDestroyerDirective } from './subscribe-destroyer.directive';

describe('SubscribeDestroyerDirective', () => {
  it('should create an instance', () => {
    const directive = new SubscribeDestroyerDirective();
    expect(directive).toBeTruthy();
  });
});
